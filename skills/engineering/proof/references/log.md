## 2026-09-16

- **Creation**: Planned and authored a bounded reference bundle for the proof skill from the complete supplied Claude conversation. Destination is `skills/engineering/proof/references`; reader job is to specify, implement, verify, and gate a given story. No previous bundle existed at this destination.
- **Creation**: Mapped all eight conversation units and load-bearing entities in `sources.md`. Checked EARS, Playwright, Scrum, and OKF claims against the primary sources cited there. Skipped background formal-method and product comparisons because the resulting skill does not use them.
- **Creation**: Kept the repository's OKF v0.1 bundle convention and documented upstream v0.2 trust distinctions without migrating other bundles. Local acceptance types and the executable JSON protocol are explicitly producer conventions.
- **Creation**: Developed the gate with Claude Fable 5.1 through the installed Claude CLI. Local integrity checks and declared actor identities are not represented as authenticated provenance or an unbypassable security boundary.
- **Update**: Local review added required finding artifacts for inspection and human criteria, unique freeze identities to invalidate reused evidence, untracked-source exclusion checks, strict run exit types, and protection against proof-file symlink writes. Verification is performed locally because the collaborator's CLI permissions did not allow its test commands.
- **Update**: Independent Fable review aligned the quickstart with the example's check IDs and clarified generated-output ignores, permitted attempt archives, and Windows command invocation. Strict OKF validation, 23 executable tests, and the 338-case blind routing evaluation passed during creation.
- **Update**: Replaced format-named destination assumptions with repository discovery. Contract placement now follows requirements ownership, documentation indexes, and OKF metadata; CLI examples take the resolved contract path explicitly. This implements the user's correction that OKF is a format, not a repository folder convention.
