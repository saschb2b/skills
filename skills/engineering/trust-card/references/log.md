# Log

## 2026-08-07

**Update.** Replaced the trading-card rendering contract with deterministic,
digest-seeded book covers. The reference renderer now selects from six abstract
print compositions, four layouts, three typographic voices, and a shared theme;
it keeps the trust gradient in a fixed colophon and continues to emit the
aggregate `cards.json` feed. Updated the reference index and the skill's visual
card guidance to match.
