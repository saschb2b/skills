# Log

## 2026-07-27

**Migration to OKF v0.2.** Bundle retargeted from `okf_version` 0.1 to 0.2. Each concept's `timestamp` became `generated: { by, at }`, carrying the original datetime and naming `claude-code/unversioned` as the producing actor, since the bundles were agent-drafted and the specific model was never recorded per file. Any `# Citations` body section moved into the `sources` frontmatter family as `{ resource, title }` entries. No `verified` events were added: nothing here has been through a recorded human or process confirmation, and asserting one would inflate the trust tier the field exists to report. Validated with `okf-validate --strict`; the migration introduced no new findings.

## 2026-07-26

**Creation and enrichment.** Bundle created with the three operating documents: the positive taxonomy of the nine comments worth writing plus the routing table, the 37-category smell catalog in six pillars, and the four-question write gate with its trigger-phrase table and rewrite recipes. The change-narration pillar and every before-and-after pair in the rewrite recipes are grounded in a survey of agent-written comments in the okf-viewer codebase (ghost references to a deleted palette and a deleted ordering, a JSX comment defending an empty-state change, a Rust doc comment narrating a sandbox policy shift, a test comment carrying its own history, a bare restatement above a draw call). Same day, the entity pass added the `concepts/` directory: eight background concepts for the load-bearing names the operating documents used without explaining (the comment's real audience, agent context collapse, the what/how/why ladder, information routing, comment decay, interface vs implementation comments, non-obvious information, commit message craft), linked from the documents with the relationships named in prose. Crawl boundary: sources are the cited comment literature (Ousterhout, Sanfilippo, the Google style and engineering-practice guides, Fowler, Atwood, Beams, the kernel style guide, and the per-ecosystem doc conventions for Rust, Go, Python, and TypeScript); no live-web crawl was run.
