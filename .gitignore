# Local skills.sh install artifacts. This repo publishes skills; it does not
# consume them, so installed copies and the lock file should not be tracked.
.agents/
.claude/
skills-lock.json

# Dependencies (dev-only tooling; skills themselves ship dependency-free).
node_modules/

# Eval run outputs (model- and time-dependent) and local secrets.
evals/results/
.env
