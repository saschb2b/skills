## 2026-09-09

* **Update**: React 19.3 (Sep 9, 2026) recorded in `frameworks/react.md`: stable `<ViewTransition>` and `addTransitionType`, `ref` on `<Fragment>`, `browser()` in `react-dom`, Trusted Types, direct `<Context>` rendering from Server Components, and independent Transition rendering; `ui/motion.md` now names the boundary against the built-in view transitions.
* **Update**: Prerelease majors flagged on 2026-08-20 that have since shipped GA, each re-verified against its release notes or migration guide: Jotai 3.0 (ESM-only, `atomFamily` to `jotai-family`, `loadable` removed), NgRx 22 (Angular 22, per-member `DeepSignal` on union slices, object-style `tapResponse` only), Vitest 5.0 (`clearMocks` default, nested projects, `vi.when`, strict locators, top-level-only `vi.mock`), pnpm 12 (Rust rewrite, `--resolution-only` removed), plus Nodemailer 10 (TypeScript rewrite, Node 20+) and Cypress 16 (in-browser interception, `cy.exec()`, `cy.end()`, and `Cypress.env()` removed).
* **Update**: Prisma corrected: 7.10 is the production line and adds `@prisma/prisma7` for side-by-side use, while the `prisma` package's npm `latest` tag now points at the 8.0.0 RC, so the note tells agents to pin `prisma@7`. Zod 4.5 and 4.6 recorded (`z.compile()`, `z.validate()`, `z.creditCard()`, `z.iban()`, standalone `@zod/mini`). Bun 1.4 recorded as the first release of the Rust port.
* **Update**: Registry-only version bumps (npm `latest` checked on 2026-09-09, official docs not re-read, so the `Verified` stamp stays 2026-08-20) in 28 notes: Anthropic SDK 0.124, OpenAI SDK 7.13, GraphQL Codegen CLI 7.4, Orval 8.30, Better Auth 1.7.3, Clerk 7.9, Sanity 6.13, Strapi 5.53, TanStack Query 5.102, React Hook Form 7.87, next-intl 4.14, Paraglide 2.25, Astro 7.3, PostHog 1.428, Sentry 10.74, OpenTelemetry experimental 0.222, Stripe 22.6, Playwright 1.63, ESLint 10.10, Nx 23.2, Storybook 10.6, Chakra 3.37, Base UI 1.8, Mantine 9.6, Motion 13.2, MUI 9.4, UnoCSS 66.10, Resend 6.26.
* **Update**: Still prerelease as of this pass, so notes keep their "do not write against yet" guidance: TanStack Query 6 (no v6 tag on npm), TanStack Start (RC), Solid 2.0 (rc.7), Vue 3.6 (rc.7), Fastify 6 (alpha), Prisma 8 (rc.13), Drizzle 1.0 (beta), Nitro 3 (beta), h3 2 (rc), React Hook Form 8 (beta), Payload 4 (canary), Storybook 11 (alpha), React Native 0.88 (rc).

## 2026-08-21

* **Update**: Cleared the last 28 orphan concepts by adding a `Companion` section to each, naming the relationship in prose (alternatives, incumbents, and layer neighbours). The bundle now passes the strict producer gate with 0 orphans and 0 broken links. Only navigation changed, so each file's `Verified` stamp stays at 2026-08-20 while its `timestamp` moves to the edit date.

## 2026-08-20

* **Update**: Full re-verification pass over all 90 concepts against official release notes and migration guides. Every note's `Verified` stamp and `timestamp` moved to 2026-08-20.
* **Update**: New majors recorded. TypeScript 7 (Go compiler, now stable), Vite 8 (Rolldown shipped), ESLint 10 (v9 end of life), npm 12 (`npm shrinkwrap` removed), Nx 23, React Router 8, Pinia 4, Nodemailer 9, Sanity Studio 6, Vercel AI SDK 7, OpenAI SDK 7, Motion 13, Prisma 7.9, Clerk Core 3.
* **Update**: Paradigm moves recorded. shadcn/ui defaults to Base UI rather than Radix, next-intl replaced `setRequestLocale` with `next/root-params`, Playwright component testing became first-party, `@angular/aria` reached stable, and tRPC gained first-party OpenAPI generation (alpha).
* **Update**: Corrected claims that had gone wrong since the last snapshot. `eslint-plugin-react-hooks` is v7 (not v6), SolidStart v2 shipped stable on Solid 1.x, Vitest 4.0 GA was October 2025, Resend batch send accepts `tags` and `scheduled_at`, tRPC is no longer OpenAPI-incapable, urql core is on 6.x, and the retired `vercel-labs/next-skills` was removed from the first-party guidance.
* **Update**: Flagged prerelease majors that agents must not write against yet. TanStack Query 6, Jotai 3, NgRx 22, Fastify 6, Prisma 8, pnpm 12, Vitest 5, and Payload 4.
* **Update**: Marked `openapi-fetch` and `openapi-react-query` as maintenance-only for 2026 per their maintainers.
* **Creation**: Added this log to the bundle.
