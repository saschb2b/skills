# Log

## 2026-08-08

**Audit.** Read-only health review of the full bundle (fingerprint ef22f2193838876c) after the enrichment below, via the okf audit capability. Deterministic layer clean: conformant in default mode, strict producer gate passed (20 concepts, 0 orphans, 0 broken links), no index drift, all footnote references keyed to sources ids, no frontmatter traps, no v0.1 leftovers. Findings, all heuristic: no freshness policy (`stale_after` unset while both sources are living, the Microsoft guide updates continuously and STE Issue 10 is scheduled for 2028); trust tier is generated-unverified throughout, correct for agent-written content, a human `verified` event is the open upgrade; `resource` unset on the four ste/ concepts whose canonical asset is the Issue 9 PDF; S1000D, ISO 3864, and Merriam-Webster explained inline rather than decomposed, tolerated at one mention each. Decision: findings recorded here without frontmatter changes, the freshness policy was considered and not adopted.

**Enrichment.** Grew the bundle from its two primary sources after the observation that one file each stood in for a whole nested guide and a whole standard. Inventory first: the Microsoft Writing Style Guide table of contents (about 980 indexed entries) and the ASD-STE100 Issue 9 structure (53 rules in 9 sections plus the dictionary). The Issue 9 PDF turned out to be publicly downloadable from the official site, so the STE material is primary-sourced from the standard itself, supplemented by the official about page and the Zambrini and Chiarello MDTT 2025 paper (STEMG members). Added the [microsoft/](microsoft/index.md) sub-bundle, seven concepts mined page by page with every rule cluster judged adopt, house, or reject against the register (punctuation, capitalization, grammar and person, word choice, procedures and instructions, numbers and acronyms, scannable content and global English). Added the [ste/](ste/index.md) sub-bundle, four concepts covering the dictionary mechanics and word rules, the multi-word-noun cap, the procedural and descriptive split with the exact rules behind the skill's modes, and safety instructions plus word-count mechanics. Corrected two numbers in [ste.md](ste.md) against the primary source, "roughly 60 rules" is 53 rules in 9 sections and "about 900 approved words" is 875 approved plus 1,274 unapproved in Issue 9, and noted the Issue 9 reclassification from specification to standard. [ste.md](ste.md) and [microsoft-style.md](microsoft-style.md) now route into their sub-bundles. Crawl boundary, recorded deliberately: the Microsoft A-Z word list (mirroring it adds bulk, not behavior; its slop-relevant entries are cited where mined), term collections, chatbot, SEO, publishing-process, and product-UI-trivia pages; on the STE side the dictionary is characterized, not reproduced, since the standard is copyrighted. Validated with `okf-validate --strict`.

**Update.** Restructured the skill around ordered top-down passes after the observation that invocations applied only the STE sentence rules and produced wording-level polish, never structural edits, and never the Microsoft mechanics. Root cause was the SKILL.md body: it carried only the construction table and the tells table, told the reviser to "scan for the tells, fix each", framed the Microsoft material as a conditional house-style mode, and contained no document-level instruction at all. The body now runs five passes in order (restructure the document, rebuild the paragraphs, build the sentences, kill the tells, check), states that a wording-only edit on a longer draft means the biggest pass was skipped, and requires reporting structural moves on someone else's draft. Added [structure.md](structure.md), the document-level pass: the reader-first outline test, the six moves (delete, merge, split, reorder, replace, cut to size) with their trigger smells, two worked outline-level examples (a generated README, a buried migration guide), the paragraph-scale versions of the moves, and the load-bearing-structure guard. Linked it from [ste.md](ste.md) (its paragraph rules are the sentence-level end of the same discipline), [prose.md](prose.md) (repairs come after structure), and the root index. Promoted the two prose-level Microsoft mechanics (verb-first statements, the hollow "you can") into the SKILL.md tells table and marked all adopted mechanics as every-mode register rules in [microsoft-style.md](microsoft-style.md), with the mechanical ones staying in [formatting.md](formatting.md). Merged the forced-enumeration and round-number tells, and the link-text and directional-reference tells, to hold the table's size. Validated with `okf-validate --strict`.

## 2026-07-30

**Update.** Added two concepts from the Microsoft Writing Style Guide and linked both from the root index. [microsoft-style.md](microsoft-style.md) records the mechanics no-slop adopts (the expletive opener, verb-first statements, the hollow "you can", heading end punctuation, the serial comma, single spacing), the two rules where Microsoft independently reaches an existing no-slop position (one word for one thing, sentence-case headings), and the three places its brand voice contradicts the skill. Those three are the em dash, which Microsoft recommends unspaced and no-slop bans; contractions, which Microsoft requires to project friendliness; and warmth itself, from which the other two follow. The conflict is genuine rather than a misreading, since Microsoft writes consumer product UI for a global audience while no-slop targets a reserved professional register, so the page resolves it with a third mode. House-style mode inverts the three when the text ships into a surface that already follows the guide, and leaves every other tell in force. [inclusive-and-accessible.md](inclusive-and-accessible.md) covers two failures the rest of the skill could not detect, prose that breaks for a reader using assistive technology and language that excludes a reader by assuming who they are. Neither reads as slop, and volume production introduces both quickly. Three of its phrase-level rules were promoted into the SKILL.md tells table (expletive opener, empty link text, directional-only reference), and two formatting rules into [formatting.md](formatting.md). Sources are the guide's top-ten, brand-voice, word-choice, accessibility, and bias-free pages, cited rather than reproduced, plus the Linguistic Society of America inclusive-language guidelines that Microsoft itself points to. No live-web crawl beyond those pages. Validated with `okf-validate --strict`.

## 2026-07-29

**Update.** Added [ste.md](ste.md), the ASD-STE100 Simplified Technical English construction layer, and linked it from the root index. The skill until now carried only a diagnostic tells table. That is the banned-word-list shape, which a first-party cross-model test measured as the least reliable fix, at a 3% slop reduction on Claude against 74% for the STE skill. The concept records the mechanical rule subset for words, verbs, sentences, and paragraphs. It also records the strict against STE-flavored mode split, the three rules where STE and no-slop disagree and which wins, the experiment table with its caveats, two real before-and-after model outputs, and the six-pass self-lint. Sources are the standard itself (cited, not reproduced, since it is free but copyrighted) and the kit that ran the experiment. No live-web crawl beyond those two. Same change added `slop-lint.mjs` at the skill root, an independent implementation of the machine-checkable subset, referenced from the self-lint section. Validated with `okf-validate --strict`.

## 2026-06-14

**Creation.** Bundle created with the per-surface guidance documents behind the skill. Prose before-and-after rewrites for each tell. Formatting tells, covering heading case, bold spam, bold-label lists, emoji, badges, and smart quotes. Code comments that narrate the diff. Commit messages and PR descriptions that restate it. Strings in code, meaning test names, error messages, and log lines.
