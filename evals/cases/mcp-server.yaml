owner: mcp-server

should_fire:
  - help me build an MCP server for our internal CLI
  - scaffold a Model Context Protocol server in TypeScript
  - I want to expose our REST API to Claude as an MCP tool, where do I start
  - add a new tool to our existing MCP server
  - how should I version our MCP server so we don't break agents that already use it
  - review the tool descriptions in our MCP server, the agent keeps calling the wrong one
  - our MCP server should use streamable HTTP instead of stdio, how do I migrate
  - is our MCP server safe against tool poisoning and token passthrough
  - the agent loads too many tools from our MCP, how do we cut the context cost
  - publish our MCP server to the registry and write a server.json

route_to_sibling:
  - prompt: generate a typesafe client from our OpenAPI spec
    expect: codegen-api

should_not_fire:
  - center this div vertically
  - rewrite this ticket as a user story
